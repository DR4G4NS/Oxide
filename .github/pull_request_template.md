## What changed

<!-- User-visible behaviour first. Internals only if they matter. A short paragraph is enough. -->

## Why

<!-- Bug, documented gap, or issue link. -->

## Tests

```text
cargo fmt --all -- --check
cargo clippy --all-targets -- -D warnings
cargo test --all-targets -- --test-threads=1
```

<!-- If you ran something else (JAR smoke, architecture_guard, a named fixture), list it. -->

## Notes

<!-- Residual vanilla gap? Wire/save IDs touched? None is fine. -->
